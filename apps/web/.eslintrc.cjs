/** @type {import("eslint").Linter.Config} */
const config = {
  root: true,
  extends: ["@koroutine/eslint-config-custom"],
};

// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
module.exports = config;
